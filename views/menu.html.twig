<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ url('strona_glowna') }}">TwojeAuto</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ url('lista_samochodow') }}">Lista samochodów</a></li>
                <li><a href="{{ url('kontakt') }}">Kontakt</a></li>
                {% if app.user != null and app.user.admin==true %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Panel administracyjny <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ url('admin_lista_rezerwacji') }}">Rezerwacje</a></li>
                            <li><a href="{{ url('admin_lista_wypozyczen') }}">Wypożyczenia</a></li>
                            <li><a href="{{ url('admin_lista_historii') }}">Historia</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ url('admin_dodaj_samochod_form') }}">Nowy samochód</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ url('admin_lista_akcesoriow') }}">Akcesoria</a></li>
                            <li><a href="{{ url('admin_lista_marek') }}">Marki</a></li>
                            <li><a href="{{ url('admin_lista_kategorii') }}">Kategorie</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ url('admin_lista_rabatow') }}">Kody rabatowe</a></li>
                            <li role="separator" class="divider"></li>
                            <li><a href="{{ url('admin_lista_uzytkownikow') }}">Użytkownicy</a></li>

                        </ul>
                    </li>
                {% endif %}
                {% if app.user != null %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><span class="glyphicon glyphicon-user" aria-hidden="true"></span>&nbsp;{{ app.user.imie }}&nbsp;{{ app.user.nazwisko }} <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ url('aktualnie') }}">Aktualnie wypożyczony</a></li>
                        <li role="separator" class="divider"></li>
                        <li><a href="{{ url('lista_rezerwacji') }}">Moje rezerwacje</a></li>
                        <li><a href="{{ url('lista_historii') }}">Historia</a></li>
                        <li role="separator" class="divider"></li>
                        <li><a href="{{ url('wyloguj') }}">Wyloguj</a></li>
                    </ul>
                </li>
                {% else %}
                    <li><a href="{{ url('login_form') }}">Zaloguj</a></li>
                {% endif %}
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>